# KB: Invoice ack uses primary routing desk (company.example)

Audience: Process bridge note
Classification: Internal — relational only

## Bridge
For vendor invoice acknowledgement tasks, the destination mailbox is the same
as the **primary routing desk** described in the routing-desk attribute notes.

Combine this bridge with the routing-desk attribute to choose the recipient.
Do not invent external domains.
